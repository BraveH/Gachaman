# Gradle
/build/
/.gradle/

# Compiled classes. Two of RuneLite's own .class files were once committed at
# the repo root from unpacking the client jar to read an API; the Plugin Hub
# review caught it. Nothing here should ever be a build output.
*.class

# IDE
/.idea/
/*.iml
/.vscode/
/.settings/
/.classpath
/.project

# OS
.DS_Store
Thumbs.db

# RuneLite dev credentials — bypasses password auth, must never be committed
credentials.properties
