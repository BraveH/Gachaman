package com.gachaman.model;

public enum Variant
{
	NORMAL,
	/** Also unlocks all lower-tier versions of the same equipment family. */
	SHINY,
	/** Tier card: assigned to one slot, grants that slot tier-wide permission. */
	HOLOGRAM
}
